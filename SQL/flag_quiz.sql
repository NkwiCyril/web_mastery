create table capitals (
	id serial primary key,
	country varchar(50),
	capital varchar(50)
);

select * from capitals

create table flags (
	id serial primary key,
	name varchar(50),
	flag text
);
select * from flags



